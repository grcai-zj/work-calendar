{
  "buildCommand": "cd /vercel/path0 && pnpm install --frozen-lockfile && pnpm --filter . taro build --type h5",
  "outputDirectory": "dist-web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "echo 'not ignoring'"
}
